spring:
  application:
    name: pdb-auth
  cloud:
    config:
      uri: https://bloodcurdling-web-94762.herokuapp.com
      fail-fast: true
      password: ${config_server_secret}
      username: user

